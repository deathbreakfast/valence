# embedded-bootstrap

Surreal embedded inventory bootstrap — discover logical DB names from linked schemas using `surreal-inventory` helpers.

## Run

```bash
cargo run -p embedded-bootstrap
```

See [`valence-backend-surreal/README.md`](../../valence-backend-surreal/README.md).
